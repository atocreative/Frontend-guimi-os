---
context: frontend-guimi-os scope-2-alignment
status: in_progress
last_updated: 2026-05-14 18:30
task_count: 5
completed: 3
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered through actual failures, not predictions.

- [ ] **GERENTE vs GESTOR**: Backend sends "GESTOR" or "DEVELOPER" but Scope 2 defines "GERENTE, SUPER_USER, ADMIN, COLABORADOR". Role mapping now handles both in auth.config.ts + feature-flags.ts. If backend changes role format, update normalizedRole mapping.
- [ ] **/api/integrations/status NOT IMPLEMENTED**: Frontend calls this endpoint on login page and elsewhere. Returns 404. BACKEND MUST IMPLEMENT in backend-guimi-os repository.
- [ ] **Comercial page /headers() error**: Route /comercial has Dynamic server usage error. Frontend issue - needs to be fixed before next build.

**Do not proceed with testing until all boxes are checked.**

---

## Current State

**Location**: C:/Users/xgame/frontend-guimi-os  
**Branch**: main  
**Build Status**: ✓ Compiled successfully (no TypeScript errors)

We are validating Scope 2 compliance for Guimicell OS frontend. Fixed 3 critical issues this session:

1. ✅ Developer role → SUPER_USER mapping
2. ✅ Removed "Indicadores" from menu + redirect route  
3. ✅ Renamed "Colaboradores" to "Ranking"

---

## Completed Work

- **FIX #1**: Developer role mapping
  - Files: auth.config.ts (role normalization)
  - Also maps GESTOR → GERENTE for backend compatibility
  - Sets isSuperUser correctly for permissions

- **FIX #2**: Indicadores page removal
  - Files: components/layout/app-sidebar.tsx (menu), app/(dashboard)/indicadores/page.tsx (route)
  - Route now redirects to "/" with comment explaining Scope 2 section 5.10
  - Build passes

- **FIX #3**: Colaboradores → Ranking
  - Files: app-sidebar.tsx (label), lib/feature-flags.ts (featureId, PAGE_ROUTES)
  - URL still /colaboradores (no breaking change)
  - Menu shows "Ranking"

---

## Remaining Work

**HIGH PRIORITY**:
1. **Backend**: Implement GET /api/integrations/status endpoint
   - Response format: `{ fone_ninja, kommo, meu_assessor }` with `status` + `last_sync`
   - Location: backend-guimi-os repository
   - Blocking: Login page + integrações page will show 404 errors

2. **Frontend**: Fix /comercial Dynamic server error
   - File: app/(dashboard)/comercial/page.tsx
   - Issue: Uses headers() in static route
   - Options: Move to dynamic route OR remove headers() usage
   - Blocking: Build warning appears during compilation

3. **Playwright Testing**: Run full test suite
   - RBAC validation across 4 roles
   - Menu visibility tests
   - Route protection tests
   - Background task: b7hsw547e still running

4. **RBAC Validation**: Test all role scenarios
   - [ ] SUPER_USER: all pages + dashboard-development
   - [ ] ADMIN: all except dashboard-development
   - [ ] GERENTE: comercial, operação, agenda, ranking (no financeiro)
   - [ ] COLABORADOR: dashboard, agenda, ranking (personal only)

5. **Dashboard validation per role**: Cards should differ
   - [ ] SUPER_USER/ADMIN: Full financeiro cards
   - [ ] GERENTE: Leads + conversão (no financeiro)
   - [ ] COLABORADOR: Personal tasks + ranking

---

## Decisions Made

- **GERENTE spelling**: Scope 2 says "GERENTE" but backend may send "GESTOR". Solution: Normalize both in auth.config.ts + feature-flags.ts to handle both gracefully.
- **Developer role**: Treated as SUPER_USER equivalent (was in codebase, not in Scope 2).
- **Indicadores removal**: Per Scope 2 section 5.10, completely removed from menu + route redirects.
- **No mock fallbacks**: Removed all silent fallbacks; pages show errors if backend fails.

---

## Blockers

| Blocker | Type | Workaround | Status |
|---------|------|-----------|--------|
| /api/integrations/status 404 | External (backend missing) | None - must implement | BLOCKING |
| /comercial Dynamic server error | Technical (Next.js) | Fix headers() usage | BLOCKING |
| Playwright b7hsw547e still running | Process | Let complete, check results next session | INFO |

---

## Human Actions Pending

1. **Backend Engineer**: Implement GET /api/integrations/status  
   - What: 3 integration status cards (Fone Ninja, Kommo, Meu Assessor)
   - Why: Frontend calls this on login + integrações pages
   - Blocking: Yes - login page shows console errors without this

2. **Frontend Engineer**: Fix /comercial Dynamic server error
   - What: Remove headers() or mark page as dynamic
   - Why: Build warning about static/dynamic mismatch
   - Blocking: Yes - build shows error

3. **Tester**: Run Playwright suite
   - What: Full RBAC + UI validation across 4 roles
   - Why: Need to confirm menu visibility + route protection works
   - Blocking: No, but critical for homologation

---

## Infrastructure State

- **Dev server**: Started but may have exited
  - Previous PID command: `npm run dev > /tmp/dev.log 2>&1 &`
  - Playwright background task: b7hsw547e running (check output at: C:\Users\xgame\AppData\Local\Temp\claude\C--Users-xgame-frontend-guimi-os\a5d56013-a4bc-4609-899f-154d8dbfe7c2\tasks\b7hsw547e.output)

- **Environment**: NEXT_PUBLIC_API_BASE_URL=http://localhost:3001 (from .env)

---

## Critical Anti-Patterns (Do NOT Repeat)

| Pattern | What Happened | Severity | Prevention |
|---------|---------------|----------|-----------|
| Silent fallbacks | Pages showed mock data instead of errors | blocking | Code now throws/shows errors if backend fails |
| Localhost hardcoding | Multiple files had localhost fallback | blocking | All routes use NEXT_PUBLIC_API_BASE_URL env var |
| Role name inconsistency | "Developer" in code vs Scope 2 "SUPER_USER" | advisory | Auth config now normalizes both; comment explains mapping |

---

## Next Action (When Resuming)

1. **Check Playwright results**: Look at b7hsw547e.output
2. **Implement /api/integrations/status in backend** (if not done)
3. **Fix /comercial Dynamic server error** (if not done)
4. **Run full Playwright test suite** with all 4 roles
5. **Update status docs** with test results
6. **Test manual flows**: Login → Different roles → Menu visibility → Route access

---

## Context & Mental State

This is rapid-iteration validation against Scope 2. Not building new features—fixing alignment issues. The 3 completed fixes are foundational for RBAC to work correctly.

Main challenge: Backend and frontend had different role nomenclature (Developer vs SUPER_USER, GESTOR vs GERENTE). Now normalized in auth layer so subsequent RBAC checks work correctly.

Next big task: Confirm menu visibility + route protection works via Playwright tests, then focus on integrations + dashboard data accuracy.

